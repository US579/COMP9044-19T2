#!/bin/bash


for file in $@
do
  echo "Address to e-mail this image to? "
  read addr
  echo "Message to accompany image?"
  read message
  mes=`echo $file | sed "s/\.jpg//"`\!
  echo '$message'|mutt -s "$mes" -e 'set copy=no' -a $message -- $addr
done

