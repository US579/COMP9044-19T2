function total_bill(bill_list) {
  // PUT YOUR CODE HERE
    var sum = 0;
    for (let i = 0 ;i< bill_list.length;i++){
        for (let j = 0 ;j < bill_list[i].length;j++){
            sum += +bill_list[i][j].price.slice(1);
        }
    }
    console.log(sum);
}

module.exports = total_bill;
